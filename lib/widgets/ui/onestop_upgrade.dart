import 'package:flutter/material.dart';

class OneStopUpgrader extends StatelessWidget {
  const OneStopUpgrader({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
