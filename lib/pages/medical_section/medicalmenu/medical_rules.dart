import 'package:flutter/material.dart';

class MedicalRules extends StatelessWidget {
  const MedicalRules({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}